# Denoising Dirty Documents — 整体解决方案规划

> 目标竞赛：Kaggle "Denoising Dirty Documents"
> 评估指标：清洗后像素与真实灰度像素的 **RMSE**（越小越好）
> 交付物： melted 格式提交文件 `id,value`（`id = image_row_col`，值域 0–1）

---

## 1. 问题分析与数据画像

| 项目 | 现状 |
|---|---|
| 训练集 | 115 对图像：`data/train/*.png`（脏图）+ `data/train_cleaned/*.png`（干净标签） |
| 测试集 | 29 张图像：`data/test/*.png` |
| 图像规格 | 统一 540×420，单通道灰度（mode=L） |
| 像素值 | 0（黑）–1（白），提交时需除以 255 归一化 |
| 噪声类型 | 灰色纹理背景、褶皱（wrinkles）、咖啡渍/暗斑（blobs）、局部阴影 |

**关键观察**（已实际查看样本）：
- 脏图的背景不是纯白，而是带纹理的灰色；目标是把背景恢复为纯白、文字保持纯黑。
- 噪声可分两类：
  1. **全局/缓慢变化噪声**：背景灰度、大面积阴影 —— 可用低频估计校正；
  2. **局部结构噪声**：褶皱边缘、墨点、污渍 —— 与文字在局部形态上相似，是难点。
- 文字为印刷体，粗细不一（斜体/粗体），因此**后处理不能做硬二值化**，否则细笔画会被抹掉或笔画断裂，RMSE 反而上升。预测应输出"软"灰度。

## 2. 总体技术路线

采用 **三级递进** 策略，每级都可独立提交验证，风险可控：

### Stage 0 — 基础设施与基线（先跑通全链路）
1. **数据管道**：读取 PNG → float32 [0,1]；建立 `image_row_col` melted 提交生成器。
2. **Baseline A（传统方法）**：背景估计（大核中值滤波/形态学闭运算）做光照归一化 + 中值去噪，输出 `clip((x - bg) / (1 - bg))` 式的校正图。预期 RMSE ~0.10–0.15，作为保底提交。
3. **验证框架**：从 115 对中划出约 15 对做验证集（固定 seed，按文件名分桶），本地计算逐像素 RMSE，模拟排行榜。

### Stage 1 — 主模型：Patch-based CNN（竞赛冠军同款思路）
把去噪建模为**逐像素回归**问题：以每个像素为中心的 patch（输入含上下文）预测中心像素的干净值。

- **网络结构**：小型 U-Net（全图输入）或 3–5 层卷积网络（patch 输入）。**首选 U-Net**：
  - 直接利用全图上下文，推理快（29 张图几秒出结果）；
  - 语义上"知道"这是文档，能区分文字笔画与褶皱；
  - 参数量小，115 张训练图够用。
- **输入特征**：原始灰度图（可附加：局部均值图、局部标准差图作为辅助通道）。
- **损失**：MSE（与评测指标一致）；输出层 sigmoid。
- **数据增强**（关键，样本量小）：
  - 随机裁剪 128×128 patch、翻转（水平/垂直）、90° 旋转；
  - 轻度亮度/对比度抖动；
  - **不做**弹性形变——文字几何结构是先验，不能破坏。
- **训练配置**：Adam(1e-3) + ReduceLROnPlateau，早停以验证 RMSE 为准；PyTorch 实现。

### Stage 2 — 提升与集成
1. **多模型集成**：训练 3–5 个不同 seed / 不同裁剪尺寸的 U-Net，预测取平均（对 RMSE 平滑有效）。
2. **TTA（测试时增强）**：翻转/旋转推理取平均。
3. **后处理（谨慎）**：
   - 仅做温和的"推向极值"：`p' = clip((p - 0.5) * k + 0.5)`，k≈1.2–1.5，把置信像素推向 0/1 但不硬截断；
   - k 在验证集上扫参确定，无收益则不用。
4. **残差思路备选**：预测 `clean = x + f(x)` 的残差，可加速收敛（备选，不改变主线）。

## 3. 验证与提交策略

- **本地验证是唯一可信信号**：每次改动必须在同一验证 split 上报 RMSE。
- 提交节奏：Baseline A → 单模型 U-Net → 集成+TTA → 后处理调参，每次提交与本地分数对照，校准验证集可信度。
- 提交文件生成：29 张测试图逐像素展开为 `image_row_col`，值域 [0,1]，约 6.6M 行（29×226,800），用流式写出 CSV 控制 内存/体积。

## 4. 里程碑与目录规划

```
denoising-dirty-documents/
├── PLAN.md                 # 本文档
├── data/                   # 已有：train / train_cleaned / test
├── src/
│   ├── common.py           # IO、RMSE、提交文件生成
│   ├── baseline.py         # Stage 0 传统方法
│   ├── dataset.py          # Dataset / 增强 / 验证split
│   ├── model.py            # U-Net 定义
│   ├── train.py            # 训练循环 + 早停
│   ├── predict.py          # 推理 + TTA + 集成
│   └── postprocess.py      # 温和极值化（可选）
├── outputs/
│   ├── ckpts/              # 模型权重
│   ├── preds/              # 预测图
│   └── submission.csv
└── experiments.md          # 每次实验的配置与验证 RMSE 记录
```

| 阶段 | 内容 | 完成标准 |
|---|---|---|
| M0 | 数据管道 + 验证 split + 提交生成器 | 能产出合法 submission.csv |
| M1 | Baseline A 本地验证 + 首次提交 | 本地 RMSE ≤ 0.15 |
| M2 | U-Net 单模型训练 | 本地 RMSE ≤ 0.06 |
| M3 | 集成 + TTA | 本地 RMSE ≤ 0.05 |
| M4 | 后处理调参 + 终版提交 | 不劣于 M3 即采纳 |

## 5. 风险与对策

| 风险 | 对策 |
|---|---|
| 训练样本少（115 张）→ 过拟合 | 强增强 + 小模型 + 早停 + 多 seed 集成 |
| 硬二值化损伤细笔画 | 只用温和的软极值化，验证不过则回退 |
| melted 提交格式错误（行列顺序） | 生成器先对一张训练图自检：用标签图生成的提交与标签逐值比对 |
| 内存/耗时 | 全图 540×420 很小，U-Net 单卡 CPU/GPU 均可训练；无瓶颈 |
| 验证集与测试分布不一致 | 验证 split 覆盖不同文件名段；对照本地/线上分数差异 |

## 6. 预期结果

- Baseline（传统方法）：本地 RMSE ≈ 0.10–0.15
- 单模型 U-Net：≈ 0.045–0.06（该赛历史冠军级水平约 0.04 上下）
- 集成 + TTA + 温和后处理：**目标本地 RMSE ≤ 0.045**

## 7. 实际进展（持续更新）

| 阶段 | 实际结果 |
|---|---|
| M0 基础设施 | ✅ 40 个测试通过；发现数据混合尺寸（420×540 ×76 + 258×540 ×39） |
| M1 Baseline | ✅ 大核中值滤波背景估计 + 白平衡归一化，验证 RMSE = **0.0499** |
| M2 U-Net 单模型 | ✅ 强化 U-Net（ResBlock+AttentionGate，19.7M 参数），seed0=0.0200 / seed1=0.0214 / seed2=0.0191 |
| M3 集成 | ✅ 三模型加权集成（0.30/0.05/0.65），验证 RMSE = **0.01895** |
| M4 后处理 | ✅ TTA 无收益、k=1.0（模型已收敛到二值）；提交文件已生成 |

**最终结果**：
- 验证集 RMSE = **0.01895**（加权集成）
- **官方评分 Score = 0.01922**（与本地验证高度一致，验证划分可靠）
- 提交文件：`outputs/submission.csv`（29 张图，5,789,880 像素，id 序列与官方 sample 逐一一致）

**关键结论**：
1. 强化 U-Net（残差 + 注意力门控）显著优于传统方法，且比历史冠军更好。
2. 数据存在两种尺寸（420×540 / 258×540），必须按原始尺寸预测。
3. 集成需加权（给强模型更高权重），简单平均会被弱模型拉低。
4. **提交格式陷阱**：官方 sampleSubmission 的 id 索引是 **1-based**（`image_1_1` 起），且 stem 用**字典序**排序——这是最容易踩坑且本地无法察觉的格式错误。
